import { useEffect } from "react";
import { PanInfo, Reorder, motion, useMotionValue } from "framer-motion";

export default function useMotionPosition(init: any) {
    return <div>usePosition</div>;
}
